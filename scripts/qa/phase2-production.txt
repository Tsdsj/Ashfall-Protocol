async (page) => {
 const errors=[],checks=[];page.on('pageerror',e=>errors.push(String(e)));await page.reload();
 const check=(ok,name,data={})=>{if(!ok)throw new Error(name);checks.push({name,pass:true,...data});};
 const records=()=>page.evaluate(()=>new Promise((resolve,reject)=>{const open=indexedDB.open('ashfall-protocol',1);open.onsuccess=()=>{const db=open.result,q=db.transaction('worlds').objectStore('worlds').getAll();q.onsuccess=()=>{db.close();resolve(q.result.filter(x=>x.name==='QA 第二阶段发布验证 R4').map(x=>({id:x.id,position:x.state.player.position,items:x.state.player.inventory.items.map(i=>[i.id,i.count])})));};q.onerror=()=>reject(q.error);};open.onerror=()=>reject(open.error);}));
 await page.getByRole('button',{name:/进入灰谷/}).waitFor();await page.screenshot({path:'docs/phase2/screenshots/menu-production.png',scale:'css'});
 check(await page.evaluate(()=>typeof window.__game==='undefined'),'生产构建没有开发调试接口');
 await page.getByRole('button',{name:/进入灰谷/}).click();await page.getByLabel('生存记录名称').fill('QA 第二阶段发布验证 R4');await page.getByLabel('世界种子',{exact:true}).fill('GREYVALE-2037');await page.getByRole('button',{name:'生成世界并开始'}).click();await page.locator('[data-action="resume"]').waitFor();await page.waitForTimeout(400);const before=(await records())[0];check(!!before,'最终发布版创建世界并保存');
 await page.bringToFront();await page.locator('[data-action="resume"]').click();await page.waitForTimeout(300);await page.keyboard.press('KeyX');await page.waitForTimeout(150);await page.keyboard.down('a');await page.waitForTimeout(800);await page.keyboard.up('a');await page.keyboard.down('w');await page.waitForTimeout(1500);await page.keyboard.up('w');await page.keyboard.press('Escape');await page.getByRole('button',{name:'保存生存记录',exact:true}).click();await page.waitForTimeout(400);const after=(await records())[0];const distance=Math.hypot(after.position.x-before.position.x,after.position.z-before.position.z);check(distance>3,'发布版真实 WASD 移动与保存',{before:before.position,after:after.position,distance});
 await page.getByRole('button',{name:'保存并返回主菜单',exact:true}).click();await page.getByRole('button',{name:'▸ 继续生存 ↗'}).waitFor();
 await page.evaluate(async()=>{await navigator.serviceWorker.ready;});await page.waitForFunction(()=>!!navigator.serviceWorker.controller);await page.waitForFunction(()=>document.querySelector('.offline-indicator')?.dataset.state==='ready',null,{timeout:90000});const cache=await page.evaluate(async()=>({keys:await caches.keys(),manifest:await(await fetch('/precache.json')).json()}));check(cache.manifest.version==='586f284659e78bb8'&&cache.keys.includes('ashfall-offline-'+cache.manifest.version),'最终构建离线资源缓存就绪',{version:cache.manifest.version,files:cache.manifest.files.length});
 await page.context().setOffline(true);try{const blocked=await page.evaluate(async()=>{try{await fetch('/uncached-qa-probe-'+Date.now(),{cache:'no-store'});return false;}catch{return true;}});check(blocked,'未缓存请求确认网络已被阻断');await page.reload();await page.getByRole('button',{name:'▸ 继续生存 ↗'}).waitFor();await page.getByRole('button',{name:'▸ 继续生存 ↗'}).click();await page.locator('[data-action="resume"]').waitFor();check(JSON.stringify((await records())[0])===JSON.stringify(after),'最终构建离线重载并恢复世界');await page.locator('[data-action="resume"]').click();await page.keyboard.down('s');await page.waitForTimeout(1400);await page.keyboard.up('s');await page.keyboard.down('a');await page.waitForTimeout(600);await page.keyboard.up('a');await page.keyboard.down('w');await page.waitForTimeout(200);await page.keyboard.up('w');await page.keyboard.press('Escape');await page.getByRole('button',{name:'保存生存记录',exact:true}).click();await page.waitForTimeout(400);const offline=(await records())[0];check(Math.hypot(offline.position.x-after.position.x,offline.position.z-after.position.z)>2,'离线世界可以继续移动和保存',{position:offline.position});await page.getByRole('button',{name:'保存并返回主菜单',exact:true}).click();}finally{await page.context().setOffline(false);}
 check(errors.length===0,'发布版页面无未处理错误',{errors});return{pass:true,origin:page.url(),checks};
}

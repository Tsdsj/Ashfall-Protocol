async(page)=>{
 const results=[],errors=[];page.on('pageerror',e=>errors.push(String(e)));
 await page.reload();await page.getByRole('button',{name:/进入灰谷/}).waitFor({timeout:60000});await page.getByRole('button',{name:/进入灰谷/}).click();await page.getByLabel('生存记录名称').fill('QA 动态事件现场');await page.getByRole('button',{name:'生成世界并开始'}).click();await page.locator('[data-action="resume"]').waitFor({timeout:60000});await page.locator('[data-action="resume"]').click();await page.waitForFunction(()=>window.__game.sim.narrative.frame().id==='opening');await page.keyboard.press('KeyX');
 await page.evaluate(()=>{const g=window.__game;g.sim.god=true;g.command('time 12');g.command('weather clear');for(const[id,n]of Object.entries({wrench:1,crowbar:1,bandage:2,water:2,electronics:5,scrap:3}))g.command('give '+id+' '+n);});
 for(const kind of ['rescue','convoy','wreck']){
  const eventId=await page.evaluate(async kind=>{const g=window.__game,{createWorldEvent}=await import('/src/simulation/world-events.ts'),{spawnHidden}=await import('/src/simulation/population.ts');g.command('teleport -14 -28');g.sim.state.player.yaw=0;g.sim.state.actors={};if(kind==='wreck'){g.sim.state.elapsed=901;g.sim.state.discovered=g.sim.gen.pois.slice(0,10).map(p=>p.id);}let event=null;for(const poi of g.sim.gen.pois){if(Math.hypot(poi.x+14,poi.z+28)>800)continue;for(const offset of [24,34,44,-24,-34,-44]){const position=g.sim.gen.position(poi.x+poi.width/2+offset,poi.z-poi.depth/2-20);event=createWorldEvent(g.sim,kind,{poiId:poi.id,position},spawnHidden,.25);if(event)break;}if(event)break;}if(!event)throw Error('no hidden event site '+kind);return event.id;},kind);
  const steps=[];
  for(let n=0;n<4;n++){
   const targetId=await page.evaluate(async eventId=>{const g=window.__game,{worldEventInteractions}=await import('/src/simulation/world-events.ts');const event=g.sim.state.events.find(e=>e.id===eventId);for(const a of Object.values(g.sim.state.actors))if(a.health>0&&Math.hypot(a.position.x-event.position.x,a.position.z-event.position.z)<38){a.health=0;a.deathTime=g.sim.state.elapsed;}const it=worldEventInteractions(g.sim).find(i=>i.id.startsWith('event:'+eventId+':'));if(!it)return null;let view=null;for(const dist of [2.2,1.5])for(const yaw of[0,Math.PI/2,Math.PI,-Math.PI/2]){const p={x:it.position.x-Math.sin(yaw)*dist,y:it.position.y,z:it.position.z-Math.cos(yaw)*dist};if(!view&&!g.sim.collision.blocked(p.x,p.y,p.z)&&g.sim.collision.visible({...p,y:p.y+1.68},{...it.position,y:it.position.y+.8}))view={p,yaw,dist};}if(!view)throw Error('no event approach '+it.id);g.game.input.unlock();g.game.input.fallback=true;g.ui.show('play');g.ui.setLocked(true);g.sim.state.player.position=view.p;g.sim.state.player.yaw=view.yaw;g.sim.state.player.pitch=Math.atan2(.88,view.dist);g.sim.verticalVelocity=0;g.sim.locomotion.reset();await g.renderer.world.stream(true);return it.id;},eventId);
   if(!targetId)break;
   await page.waitForTimeout(500);const actual=await page.evaluate(()=>window.__game.renderer.target());if(actual?.id!==targetId)throw Error('wrong event target '+targetId+' '+JSON.stringify(actual));await page.screenshot({path:'docs/phase2/screenshots/event-'+kind+'-'+n+'.png',scale:'css'});await page.keyboard.press('KeyE');await page.waitForFunction(()=>!window.__game.sim.actions.pending);await page.waitForTimeout(100);steps.push(await page.evaluate(eventId=>window.__game.sim.state.events.find(e=>e.id===eventId).encounter,eventId));
   if(kind==='rescue'&&n===0)await page.waitForFunction(eventId=>window.__game.sim.state.events.find(e=>e.id===eventId).encounter.stage>=2,eventId,{timeout:20000});
  }
  const state=await page.evaluate(eventId=>window.__game.sim.state.events.find(e=>e.id===eventId),eventId);if(!state.resolved||state.encounter.outcome!=='success')throw Error('event incomplete '+JSON.stringify(state));results.push({kind,steps,state});
 }
 await page.keyboard.press('Escape');return{setup:'Events are created through the actual hidden-site spawner. Developer supplied tools/costs and threat removal isolate physical E interactions and stage/reward results. The rare-wreck fixture has its 15-minute/10-discovery eligibility pre-set; rarity and unmet gates are independently unit-tested.',results,errors};
}
